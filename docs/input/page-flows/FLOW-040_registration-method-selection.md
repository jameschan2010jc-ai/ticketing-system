# FLOW-040 Registration Method Selection

1. User lands on `/register-method` (p40) from p2 second option.
2. User sees two registration options: phone and email.
3. If phone registration is selected, system routes to `/register-phone` (p41).
4. If email registration is selected, system routes to `/register-email` (p42).
5. Header back arrow returns to `/purchase-method` (p2).
